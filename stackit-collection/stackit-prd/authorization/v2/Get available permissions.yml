info:
  name: Get available permissions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v2/permissions"
  params:
    - name: resourceType
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get available permissions
