meta {
  name: Retrieve the list of TLS certificate stored in a project.
  type: http
  seq: 1
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta/projects/:projectId/regions/:region/certificates
  body: none
  auth: inherit
}

params:query {
  ~pageSize: 
  ~pageId: 
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
}
