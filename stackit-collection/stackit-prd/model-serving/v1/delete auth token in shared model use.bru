meta {
  name: delete auth token in shared model use
  type: http
  seq: 3
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:regionId/tokens/:tId
  body: none
  auth: inherit
}
