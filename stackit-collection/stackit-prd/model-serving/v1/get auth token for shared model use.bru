meta {
  name: get auth token for shared model use
  type: http
  seq: 4
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/regions/{{regionId}}/tokens/{{tId}}
  body: none
  auth: inherit
}
