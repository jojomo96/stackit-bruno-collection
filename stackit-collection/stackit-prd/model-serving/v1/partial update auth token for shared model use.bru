meta {
  name: partial update auth token for shared model use
  type: http
  seq: 5
}

auth {
  mode: inherit
}

patch {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:regionId/tokens/:tId
  body: json
  auth: inherit
}

body:json {
  {
    "description": "my token",
    "name": "my-token"
  }
}
