meta {
  name: Get a single rrset
  type: http
  seq: 21
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/zones/:zoneId/rrsets/:rrSetId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  zoneId: {{zoneId}}
  rrSetId: {{rrSetId}}
}
