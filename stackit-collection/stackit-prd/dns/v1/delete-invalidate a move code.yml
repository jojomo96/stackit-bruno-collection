info:
  name: delete/invalidate a move code
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/v1/projects/:projectId/zones/:zoneId/move-code"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
    - name: zoneId
      value: ""
      type: path
      description: zone id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: delete/invalidate a move code
