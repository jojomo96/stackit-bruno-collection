meta {
  name: Restore record set
  type: http
  seq: 24
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/zones/:zoneId/rrsets/:rrSetId/restores
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  zoneId: {{zoneId}}
  rrSetId: {{rrSetId}}
}
