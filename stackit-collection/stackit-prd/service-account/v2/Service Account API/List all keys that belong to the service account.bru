meta {
  name: List all keys that belong to the service account
  type: http
  seq: 12
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/projects/:projectId/service-accounts/:serviceAccountEmail/keys
  body: none
  auth: inherit
}
