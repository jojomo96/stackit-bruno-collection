meta {
  name: Get service account key details
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/v2/projects/:projectId/service-accounts/:serviceAccountEmail/keys/:keyId
  body: none
  auth: inherit
}

params:query {
  format: X509_PEM
}
