info:
  name: Create a new Federated Identity Provider
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/v2/projects/:projectId/service-accounts/:serviceAccountEmail/federations"
  params:
    - name: projectId
      value: ""
      type: path
      description: The ID of the project.
    - name: serviceAccountEmail
      value: ""
      type: path
      description: The email of the service account.
  body:
    type: json
    data: |-
      {
        "assertions": [
          {
            "item": "",
            "operator": "",
            "value": ""
          }
        ],
        "issuer": "",
        "name": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Federate the service account with an identity provider.
