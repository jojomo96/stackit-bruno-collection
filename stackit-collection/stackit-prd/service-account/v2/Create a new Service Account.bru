meta {
  name: Create a new Service Account
  type: http
  seq: 3
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2/projects/:projectId/service-accounts
  body: json
  auth: inherit
}

body:json {
  {
    "name": "my-sa-prefix"
  }
}
