meta {
  name: Delete Service Account
  type: http
  seq: 4
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/projects/{{projectId}}/service-accounts/{{serviceAccountEmail}}
  body: none
  auth: inherit
}
