meta {
  name: Update Access Token
  type: http
  seq: 12
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v1/projects/{{projectId}}/regions/{{regionId}}/instances/{{instanceId}}/access-tokens/{{tId}}
  body: json
  auth: inherit
}

body:json {
  {
    "description": "",
    "displayName": ""
  }
}
