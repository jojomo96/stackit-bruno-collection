meta {
  name: Deletes all expired access tokens
  type: http
  seq: 9
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1alpha/projects/:projectId/regions/:regionId/instances/:instanceId/access-tokens/expired
  body: none
  auth: inherit
}
