meta {
  name: Delete Access Token
  type: http
  seq: 10
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:regionId/instances/:instanceId/access-tokens/:tId
  body: none
  auth: inherit
}
