meta {
  name: delete-intake-runner
  type: http
  seq: 3
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:regionId/intake-runners/:intakeRunnerId
  body: none
  auth: inherit
}

params:query {
  force: false
}
