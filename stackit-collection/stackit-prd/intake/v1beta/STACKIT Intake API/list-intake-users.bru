meta {
  name: list-intake-users
  type: http
  seq: 11
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:regionId/intakes/:intakeId/users
  body: none
  auth: inherit
}

params:query {
  pageSize: 10
  ~pageToken: 
}
