meta {
  name: disable backup service
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/servers/:serverId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  serverId: {{serverId}}
}
