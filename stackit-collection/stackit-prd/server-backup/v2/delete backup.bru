meta {
  name: delete backup
  type: http
  seq: 9
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/backups/:backupId
  body: none
  auth: inherit
}

params:query {
  ~force: 
}

params:path {
  projectId: {{projectId}}
  serverId: {{serverId}}
  region: {{region}}
  backupId: {{backupId}}
}
