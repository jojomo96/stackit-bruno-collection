meta {
  name: Complete cluster credentials rotation
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/clusters/:clusterName/complete-credentials-rotation
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  clusterName: {{clusterName}}
}
