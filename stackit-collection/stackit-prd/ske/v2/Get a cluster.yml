info:
  name: Get a cluster
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v2/projects/:projectId/regions/:region/clusters/:clusterName"
  params:
    - name: projectId
      value: ""
      type: path
    - name: region
      value: ""
      type: path
    - name: clusterName
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get Kubernetes cluster for the specified identifier, belonging to the project specified by `projectId`.
