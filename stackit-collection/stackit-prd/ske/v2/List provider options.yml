info:
  name: List provider options
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/v2/regions/:region/provider-options"
  params:
    - name: region
      value: ""
      type: path
    - name: versionState
      value: SUPPORTED
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns available Kubernetes versions, availability zones, machine types, OS versions and volume types for the cluster nodes.
