info:
  name: Start cluster credentials rotation
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/v2/projects/:projectId/regions/:region/clusters/:clusterName/start-credentials-rotation"
  params:
    - name: projectId
      value: ""
      type: path
    - name: region
      value: ""
      type: path
    - name: clusterName
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Start cluster credentials rotation. This is step 1 of a two-step process. Complete the rotation using [complete-credentials-rotation](#tag/Credentials/operation/SkeService_CompleteClusterCredentialsRotation).
