meta {
  name: Start cluster credentials rotation
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/clusters/:clusterName/start-credentials-rotation
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  clusterName: {{clusterName}}
}
