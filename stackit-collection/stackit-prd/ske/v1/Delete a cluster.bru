meta {
  name: Delete a cluster
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/clusters/:clusterName
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  clusterName: {{clusterName}}
}
