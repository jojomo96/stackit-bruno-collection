info:
  name: List provider options
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/v1/provider-options"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns a list of supported Kubernetes versions and a list of supported machine types for the cluster nodes.
