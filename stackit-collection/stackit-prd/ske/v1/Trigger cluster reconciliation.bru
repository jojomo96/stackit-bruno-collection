meta {
  name: Trigger cluster reconciliation
  type: http
  seq: 10
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/{{projectId}}/clusters/{{clusterName}}/reconcile
  body: none
  auth: inherit
}

params:path {
  projectId: 
  clusterName: 
}
