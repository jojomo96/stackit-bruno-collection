meta {
  name: Get Instance information.
  type: http
  seq: 5
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1beta/projects/{{projectId}}/instances/{{instanceId}}
  body: none
  auth: inherit
}
