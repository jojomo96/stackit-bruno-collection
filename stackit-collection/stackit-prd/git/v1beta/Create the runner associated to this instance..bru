meta {
  name: Create the runner associated to this instance.
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/v1beta/projects/:projectId/instances/:instanceId/runner
  body: json
  auth: inherit
}

body:json {
  {
    "labels": [
      "stackit-docker"
    ]
  }
}
