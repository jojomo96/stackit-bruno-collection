info:
  name: Get Flavors
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/flavors"
  params:
    - name: projectId
      value: ""
      type: path
      description: Project ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get available flavors for a specific projectID
