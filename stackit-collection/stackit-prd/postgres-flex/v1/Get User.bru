meta {
  name: Get User
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/users/:userId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  instanceId: {{instanceId}}
  userId: {{userId}}
}
