meta {
  name: Delete User
  type: http
  seq: 20
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/users/:userId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  instanceId: {{instanceId}}
  userId: {{userId}}
}
