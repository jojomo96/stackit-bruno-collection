info:
  name: Reset User
  type: http
  seq: 24

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/users/:userId/reset"
  params:
    - name: projectId
      value: ""
      type: path
      description: Project ID
    - name: instanceId
      value: ""
      type: path
      description: Instance ID
    - name: userId
      value: ""
      type: path
      description: user ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Reset user password for a postgres instance
