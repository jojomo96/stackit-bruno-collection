meta {
  name: Reset User
  type: http
  seq: 24
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/users/:userId/reset
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  instanceId: {{instanceId}}
  userId: {{userId}}
}
