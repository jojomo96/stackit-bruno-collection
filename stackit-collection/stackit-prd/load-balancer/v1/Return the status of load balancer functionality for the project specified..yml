info:
  name: Return the status of load balancer functionality for the project specified.
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId"
  params:
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  DEPRECATED! Checking the status is now obsolete. The endpoint is kept for compatibility.
   Status will return the load balancer functionality status for the project specified.
