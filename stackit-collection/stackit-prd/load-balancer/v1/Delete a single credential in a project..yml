info:
  name: Delete a single credential in a project.
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/v1/projects/:projectId/credentials/:credentialsRef"
  params:
    - name: projectId
      value: ""
      type: path
    - name: credentialsRef
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete Credential
