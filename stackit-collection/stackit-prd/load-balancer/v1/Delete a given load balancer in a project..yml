info:
  name: Delete a given load balancer in a project.
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{baseUrl}}/v1/projects/:projectId/load-balancers/:name"
  params:
    - name: projectId
      value: ""
      type: path
    - name: name
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: DeleteLoadBalancer will delete a given load balancer.
