meta {
  name: List all credentials in a project.
  type: http
  seq: 5
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/credentials
  body: none
  auth: inherit
}

params:path {
  projectId: 
}
