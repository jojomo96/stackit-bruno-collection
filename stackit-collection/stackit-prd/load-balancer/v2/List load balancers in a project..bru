meta {
  name: List load balancers in a project.
  type: http
  seq: 6
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/load-balancers
  body: none
  auth: inherit
}

params:query {
  ~pageSize: 
  ~pageId: 
}

params:path {
  projectId: 
  region: 
}
