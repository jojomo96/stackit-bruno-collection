info:
  name: List available service plans.
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/v2/regions/:region/plans"
  params:
    - name: region
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: ListPlans returns for the configured service plans for a project.
