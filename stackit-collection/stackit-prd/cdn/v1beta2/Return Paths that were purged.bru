meta {
  name: Return Paths that were purged
  type: http
  seq: 7
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1beta2/projects/:projectId/distributions/:distributionId/cache/paths
  body: none
  auth: inherit
}

params:query {
  path: 
}

params:path {
  projectId: {{projectId}}
  distributionId: {{distributionId}}
}
