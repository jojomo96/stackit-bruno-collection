meta {
  name: trigger a recreate
  type: http
  seq: 16
}

patch {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/recreate
  body: none
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
