info:
  name: patch-organization-telemetry-link
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}/v1beta/organizations/:organizationId/regions/:regionId/telemetry-link"
  params:
    - name: organizationId
      value: ""
      type: path
      description: The STACKIT portal organization UUID the resource is located in.
    - name: regionId
      value: eu01
      type: path
      description: The STACKIT region name the resource is located in.
    - name: regionId
      value: eu02
      type: path
      description: The STACKIT region name the resource is located in.
      disabled: true
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Patches the given Telemetry Link within the organization.
