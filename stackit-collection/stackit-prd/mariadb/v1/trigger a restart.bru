meta {
  name: trigger a restart
  type: http
  seq: 17
}

patch {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/restart
  body: none
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
