meta {
  name: Destroy version
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/versions/:versionNumber/destroy
  body: none
  auth: inherit
}
