meta {
  name: Delete keyring
  type: http
  seq: 3
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/{{projectId}}/regions/{{regionId}}/keyrings/{{keyRingId}}
  body: none
  auth: inherit
}
