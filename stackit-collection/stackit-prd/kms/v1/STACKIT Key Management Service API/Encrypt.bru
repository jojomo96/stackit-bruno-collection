meta {
  name: Encrypt
  type: http
  seq: 18
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/versions/:versionNumber/encrypt
  body: json
  auth: inherit
}

body:json {
  {
    "data": ""
  }
}
