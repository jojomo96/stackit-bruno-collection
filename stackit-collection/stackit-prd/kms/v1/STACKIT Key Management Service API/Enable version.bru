meta {
  name: Enable version
  type: http
  seq: 17
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/versions/:versionNumber/enable
  body: none
  auth: inherit
}
