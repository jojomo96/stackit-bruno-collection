meta {
  name: Delete key
  type: http
  seq: 7
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId
  body: none
  auth: inherit
}
