meta {
  name: Rotate key
  type: http
  seq: 11
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/{{projectId}}/regions/{{regionId}}/keyrings/{{keyRingId}}/keys/{{keyId}}/rotate
  body: none
  auth: inherit
}
