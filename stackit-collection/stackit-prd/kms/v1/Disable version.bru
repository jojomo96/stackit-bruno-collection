meta {
  name: Disable version
  type: http
  seq: 16
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/versions/:versionNumber/disable
  body: none
  auth: inherit
}
