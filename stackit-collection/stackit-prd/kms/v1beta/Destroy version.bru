meta {
  name: Destroy version
  type: http
  seq: 15
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1beta/projects/{{projectId}}/regions/{{regionId}}/keyrings/{{keyRingId}}/keys/{{keyId}}/versions/{{versionNumber}}/destroy
  body: none
  auth: inherit
}
