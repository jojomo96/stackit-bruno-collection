meta {
  name: List wrapping keys
  type: http
  seq: 22
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1beta/projects/{{projectId}}/regions/{{regionId}}/keyrings/{{keyRingId}}/wrappingkeys
  body: none
  auth: inherit
}
