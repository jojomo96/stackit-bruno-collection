meta {
  name: Sign
  type: http
  seq: 20
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/versions/:versionNumber/sign
  body: json
  auth: inherit
}

body:json {
  {
    "data": ""
  }
}
