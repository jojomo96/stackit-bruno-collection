meta {
  name: Import key
  type: http
  seq: 9
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/import
  body: json
  auth: inherit
}

body:json {
  {
    "wrappedKey": "",
    "wrappingKeyId": ""
  }
}
