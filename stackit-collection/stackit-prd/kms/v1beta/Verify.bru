meta {
  name: Verify
  type: http
  seq: 21
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1beta/projects/{{projectId}}/regions/{{regionId}}/keyrings/{{keyRingId}}/keys/{{keyId}}/versions/{{versionNumber}}/verify
  body: json
  auth: inherit
}

body:json {
  {
    "data": "",
    "signature": ""
  }
}
