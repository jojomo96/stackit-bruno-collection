meta {
  name: Encrypt
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/versions/:versionNumber/encrypt
  body: json
  auth: inherit
}

body:json {
  {
    "data": ""
  }
}
