meta {
  name: Restore deleted key
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:regionId/keyrings/:keyRingId/keys/:keyId/restore
  body: none
  auth: inherit
}
