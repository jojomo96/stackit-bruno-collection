meta {
  name: Get key ring
  type: http
  seq: 4
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1beta/projects/{{projectId}}/regions/{{regionId}}/keyrings/{{keyRingId}}
  body: none
  auth: inherit
}
