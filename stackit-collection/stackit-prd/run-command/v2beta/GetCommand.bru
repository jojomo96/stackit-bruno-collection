meta {
  name: GetCommand
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/v2beta/projects/:projectId/regions/:region/servers/:serverId/commands/:commandId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  serverId: {{serverId}}
  commandId: {{commandId}}
}
