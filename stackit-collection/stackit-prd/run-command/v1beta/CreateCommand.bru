meta {
  name: CreateCommand
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v1beta/projects/:projectId/servers/:serverId/commands
  body: json
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  serverId: {{serverId}}
}

body:json {
  {
    "commandTemplateName": "RunShellScript",
    "parameters": {
      "script": "echo hello-world"
    }
  }
}
