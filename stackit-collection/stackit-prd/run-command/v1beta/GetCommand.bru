meta {
  name: GetCommand
  type: http
  seq: 5
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1beta/projects/:projectId/servers/:serverId/commands/:commandId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  serverId: {{serverId}}
  commandId: {{commandId}}
}
