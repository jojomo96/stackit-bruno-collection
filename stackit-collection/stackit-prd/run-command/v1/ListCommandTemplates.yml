info:
  name: ListCommandTemplates
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/command-templates"
  params:
    - name: osType
      value: ""
      type: query
      description: The type of the Operating System (windows or linux). If not provided will return data for all OS types.
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns a list of command templates
