meta {
  name: PartialUpdateAlertRecords
  type: http
  seq: 37
}

auth {
  mode: inherit
}

patch {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertgroups/:groupName/records
  body: json
  auth: inherit
}

params:path {
  groupName: {{groupName}}
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}

body:json {
  [
    {
      "expr": "",
      "labels": {},
      "record": ""
    }
  ]
}
