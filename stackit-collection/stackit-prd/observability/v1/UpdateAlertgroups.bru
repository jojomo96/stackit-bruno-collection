meta {
  name: UpdateAlertgroups
  type: http
  seq: 24
}

put {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertgroups
  body: json
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}

body:json {
  [
    {
      "interval": "60s",
      "name": "string",
      "rules": [
        {
          "alert": "string",
          "annotations": {},
          "expr": "string",
          "for": "0s",
          "labels": {}
        },
        {
          "expr": "string",
          "labels": {},
          "record": "string"
        }
      ]
    }
  ]
}
