info:
  name: ListRedisChecks
  type: http
  seq: 93

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/redis-checks"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all redis checks configured.
