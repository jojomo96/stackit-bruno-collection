meta {
  name: DeleteAlertRecords
  type: http
  seq: 35
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertgroups/:groupName/records
  body: none
  auth: inherit
}

params:query {
  alertRecord: []
}

params:path {
  groupName: {{groupName}}
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
