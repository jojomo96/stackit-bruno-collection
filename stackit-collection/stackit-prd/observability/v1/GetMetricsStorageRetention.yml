info:
  name: GetMetricsStorageRetention
  type: http
  seq: 73

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/metrics-storage-retentions"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get metric storage retention time.
