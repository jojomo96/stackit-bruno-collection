meta {
  name: DeleteAlertConfigReceiver
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertconfigs/receivers/:receiver
  body: none
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
  receiver: {{receiver}}
}
