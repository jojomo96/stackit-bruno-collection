meta {
  name: CreateNetworkCheck
  type: http
  seq: 82
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/network-checks
  body: json
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}

body:json {
  {
    "address": ""
  }
}
