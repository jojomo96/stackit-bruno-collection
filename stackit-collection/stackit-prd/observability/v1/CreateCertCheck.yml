info:
  name: CreateCertCheck
  type: http
  seq: 49

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/cert-checks"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "source": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create cert check.
