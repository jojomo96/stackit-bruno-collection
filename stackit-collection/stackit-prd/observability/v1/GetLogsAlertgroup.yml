info:
  name: GetLogsAlertgroup
  type: http
  seq: 69

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/logs-alertgroups/:groupName"
  params:
    - name: groupName
      value: ""
      type: path
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get logs alert group config.
