info:
  name: ListRabbitmqChecks
  type: http
  seq: 90

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/rabbitmq-checks"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all rabbitmq checks configured.
