info:
  name: ListAlertgroups
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertgroups"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get alert groups.
