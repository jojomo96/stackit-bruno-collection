info:
  name: CreateMysqlCheck
  type: http
  seq: 79

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/mysql-checks"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "server": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create mysql check.
