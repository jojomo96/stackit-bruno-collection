meta {
  name: DeleteElasticsearchCheck
  type: http
  seq: 60
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/elasticsearch-checks/:id
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
