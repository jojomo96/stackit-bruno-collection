info:
  name: ListOfferings
  type: http
  seq: 105

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/offerings"
  params:
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all plans.
