meta {
  name: DeleteAlertConfigRoute
  type: http
  seq: 17
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertconfigs/routes/:receiver
  body: none
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
  receiver: {{receiver}}
}
