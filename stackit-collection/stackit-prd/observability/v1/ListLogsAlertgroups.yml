info:
  name: ListLogsAlertgroups
  type: http
  seq: 66

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/logs-alertgroups"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get logs alert groups config.
