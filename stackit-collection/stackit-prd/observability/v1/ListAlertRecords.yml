info:
  name: ListAlertRecords
  type: http
  seq: 36

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertgroups/:groupName/records"
  params:
    - name: groupName
      value: ""
      type: path
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get alert records.
