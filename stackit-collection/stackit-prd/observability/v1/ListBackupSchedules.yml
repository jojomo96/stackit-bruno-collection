info:
  name: ListBackupSchedules
  type: http
  seq: 44

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/backup-schedules"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
    - name: backupTarget
      value: alertConfig
      type: query
      description: List of backup targets
      disabled: true
    - name: backupTarget
      value: alertRules
      type: query
      description: List of backup targets
      disabled: true
    - name: backupTarget
      value: scrapeConfig
      type: query
      description: List of backup targets
      disabled: true
    - name: backupTarget
      value: grafana
      type: query
      description: List of backup targets
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get backup schedules.
