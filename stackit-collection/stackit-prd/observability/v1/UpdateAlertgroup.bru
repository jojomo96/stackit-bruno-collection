meta {
  name: UpdateAlertgroup
  type: http
  seq: 27
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v1/projects/{{projectId}}/instances/{{instanceId}}/alertgroups/{{groupName}}
  body: json
  auth: inherit
}

params:path {
  groupName: 
  instanceId: 
  projectId: 
}

body:json {
  [
    {
      "interval": "60s",
      "name": "string",
      "rules": [
        {
          "alert": "string",
          "annotations": {},
          "expr": "string",
          "for": "0s",
          "labels": {}
        },
        {
          "expr": "string",
          "labels": {},
          "record": "string"
        }
      ]
    }
  ]
}
