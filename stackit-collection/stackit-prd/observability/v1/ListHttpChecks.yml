info:
  name: ListHttpChecks
  type: http
  seq: 63

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/http-checks"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all http checks configured.
