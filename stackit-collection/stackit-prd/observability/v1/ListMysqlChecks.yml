info:
  name: ListMysqlChecks
  type: http
  seq: 78

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/mysql-checks"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all mysql checks configured.
