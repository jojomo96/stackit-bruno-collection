info:
  name: UpdateAlertgroup
  type: http
  seq: 27

http:
  method: PUT
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertgroups/:groupName"
  params:
    - name: groupName
      value: ""
      type: path
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  body:
    type: json
    data: |-
      [
        {
          "interval": "60s",
          "name": "string",
          "rules": [
            {
              "alert": "string",
              "annotations": {},
              "expr": "string",
              "for": "0s",
              "labels": {}
            },
            {
              "expr": "string",
              "labels": {},
              "record": "string"
            }
          ]
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Update alert group config.
