meta {
  name: DeleteInstance
  type: http
  seq: 3
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId
  body: none
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
