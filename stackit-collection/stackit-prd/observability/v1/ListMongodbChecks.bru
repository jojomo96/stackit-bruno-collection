meta {
  name: ListMongodbChecks
  type: http
  seq: 75
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/mongodb-checks
  body: none
  auth: inherit
}

params:path {
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
