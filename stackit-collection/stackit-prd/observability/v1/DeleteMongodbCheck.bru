meta {
  name: DeleteMongodbCheck
  type: http
  seq: 77
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/mongodb-checks/:id
  body: none
  auth: inherit
}

params:path {
  id: {{id}}
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
