info:
  name: GetAlertConfigs
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertconfigs"
  params:
    - name: instanceId
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get alert config.
