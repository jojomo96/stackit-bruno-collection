meta {
  name: DeleteAlertrules
  type: http
  seq: 28
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/alertgroups/:groupName/alertrules
  body: none
  auth: inherit
}

params:query {
  alertName: []
}

params:path {
  groupName: {{groupName}}
  instanceId: {{instanceId}}
  projectId: {{projectId}}
}
