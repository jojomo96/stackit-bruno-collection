meta {
  name: Get product by ID
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/v2/products/:productId
  body: none
  auth: inherit
}

params:query {
  ~language: en
  ~language: de
}

params:path {
  productId: {{productId}}
}
