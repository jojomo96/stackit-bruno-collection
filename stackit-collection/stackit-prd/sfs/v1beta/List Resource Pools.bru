meta {
  name: List Resource Pools
  type: http
  seq: 1
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1beta/projects/:projectId/regions/:region/resourcePools
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
}
