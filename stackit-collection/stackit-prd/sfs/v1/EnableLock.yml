info:
  name: EnableLock
  type: http
  seq: 27

http:
  method: POST
  url: "{{baseUrl}}/v1/regions/:region/project/:projectId/locks"
  params:
    - name: region
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Enable Lock for a project. Necessary for immutable snapshots and to prevent accidental deletion of resources.
