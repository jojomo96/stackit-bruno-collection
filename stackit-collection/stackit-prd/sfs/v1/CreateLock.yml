info:
  name: CreateLock
  type: http
  seq: 25

http:
  method: POST
  url: "{{baseUrl}}/v1/regions/:region/project/:projectId/locks"
  params:
    - name: region
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create Lock
