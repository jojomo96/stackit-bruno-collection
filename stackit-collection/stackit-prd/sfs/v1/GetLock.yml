info:
  name: GetLock
  type: http
  seq: 26

http:
  method: GET
  url: "{{baseUrl}}/v1/regions/:region/project/:projectId/locks"
  params:
    - name: region
      value: ""
      type: path
    - name: projectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/v1/regions/:region/project/:projectId/locks"
      method: GET
      params:
        - name: region
          value: ""
          type: path
        - name: projectId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "lockId": ""
          }

docs: Get lock status for a project.
