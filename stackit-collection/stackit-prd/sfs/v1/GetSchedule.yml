info:
  name: GetSchedule
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/schedules/:id"
  params:
    - name: projectId
      value: ""
      type: path
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get Schedule
