meta {
  name: Get Share
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/regions/:region/resourcePools/:resourcePoolId/shares/:shareId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  resourcePoolId: {{resourcePoolId}}
  shareId: {{shareId}}
}
