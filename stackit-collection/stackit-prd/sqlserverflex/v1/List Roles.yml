info:
  name: List Roles
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/roles"
  params:
    - name: projectId
      value: ""
      type: path
      description: Project ID
    - name: instanceId
      value: ""
      type: path
      description: Instance ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List available roles for an instance that can be assigned to a user
