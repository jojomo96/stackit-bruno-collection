info:
  name: List current running restore jobs
  type: http
  seq: 19

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/restores"
  params:
    - name: projectId
      value: ""
      type: path
      description: Project ID
    - name: instanceId
      value: ""
      type: path
      description: Instance ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all currently running restore jobs which are available for a specific instance
