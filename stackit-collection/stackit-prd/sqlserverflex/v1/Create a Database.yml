info:
  name: Create a Database
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/databases"
  params:
    - name: projectId
      value: ""
      type: path
      description: Project ID
    - name: instanceId
      value: ""
      type: path
      description: Instance ID
  body:
    type: json
    data: |-
      {
        "name": "Test",
        "options": {
          "collation": "Latin1_General_100_CI_AS_KS_WS",
          "compatibilityLevel": "160",
          "owner": "Herbert"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create a Database for an instance
