info:
  name: Get list of databases
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/databases"
  params:
    - name: projectId
      value: ""
      type: path
      description: Project ID
    - name: instanceId
      value: ""
      type: path
      description: Instance ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get list of all databases in the instance
