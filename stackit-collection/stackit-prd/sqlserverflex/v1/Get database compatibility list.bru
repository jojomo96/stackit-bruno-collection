meta {
  name: Get database compatibility list
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/compatibility
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  instanceId: {{instanceId}}
}
