meta {
  name: Create a Restore Operation
  type: http
  seq: 20
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v3beta1/projects/:projectId/regions/:region/instances/:instanceId/restores
  body: json
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: eu01
  instanceId: {{instanceId}}
}

body:json {
  {
    "database_name": "customer_production_db",
    "source": {
      "type": "BACKUP"
    }
  }
}
