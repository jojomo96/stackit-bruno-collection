meta {
  name: Detach a service account from a server.
  type: http
  seq: 99
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/{{projectId}}/servers/{{serverId}}/service-accounts/{{serviceAccountMail}}
  body: none
  auth: inherit
}
