meta {
  name: List all security groups inside a project.
  type: http
  seq: 69
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/security-groups
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
