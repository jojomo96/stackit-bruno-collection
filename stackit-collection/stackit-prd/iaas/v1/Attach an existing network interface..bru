meta {
  name: Attach an existing network interface.
  type: http
  seq: 90
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v1/projects/:projectId/servers/:serverId/nics/:nicId
  body: none
  auth: inherit
}
