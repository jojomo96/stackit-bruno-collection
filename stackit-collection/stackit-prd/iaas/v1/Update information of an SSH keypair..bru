meta {
  name: Update information of an SSH keypair.
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/v1/keypairs/:keypairName
  body: json
  auth: inherit
}

body:json {
  {
    "labels": {
      "key": "value"
    }
  }
}
