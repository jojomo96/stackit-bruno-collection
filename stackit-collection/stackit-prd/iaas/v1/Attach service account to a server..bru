meta {
  name: Attach service account to a server.
  type: http
  seq: 100
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v1/projects/{{projectId}}/servers/{{serverId}}/service-accounts/{{serviceAccountMail}}
  body: none
  auth: inherit
}
