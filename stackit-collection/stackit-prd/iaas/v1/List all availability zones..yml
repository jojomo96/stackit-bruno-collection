info:
  name: List all availability zones.
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/availability-zones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a list of all availability zones.
