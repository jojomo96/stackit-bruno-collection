info:
  name: Deallocate an existing server.
  type: http
  seq: 84

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/servers/:serverId/deallocate"
  params:
    - name: projectId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Project.
    - name: serverId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Server.
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Deallocate an existing server. The server will be removed from the hypervisor so only the volume will be billed.
