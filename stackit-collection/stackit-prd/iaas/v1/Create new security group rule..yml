info:
  name: Create new security group rule.
  type: http
  seq: 75

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/security-groups/:securityGroupId/rules"
  params:
    - name: projectId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Project.
    - name: securityGroupId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Security Group.
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create a new security group rule in a project.
