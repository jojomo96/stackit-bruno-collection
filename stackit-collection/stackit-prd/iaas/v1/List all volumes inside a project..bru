meta {
  name: List all volumes inside a project.
  type: http
  seq: 116
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/volumes
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
