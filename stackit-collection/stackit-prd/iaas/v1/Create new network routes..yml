info:
  name: Create new network routes.
  type: http
  seq: 19

http:
  method: POST
  url: "{{baseUrl}}/v1/organizations/:organizationId/network-areas/:areaId/routes"
  params:
    - name: organizationId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Organization.
    - name: areaId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Network Area.
  body:
    type: json
    data: |-
      {
        "ipv4": [
          {
            "nexthop": "10.1.2.10",
            "prefix": "192.168.0.0/24"
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create one or several new network routes in a network area.
