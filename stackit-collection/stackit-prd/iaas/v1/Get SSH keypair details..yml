info:
  name: Get SSH keypair details.
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/v1/keypairs/:keypairName"
  params:
    - name: keypairName
      value: keypair-123
      type: path
      description: The name of an SSH keypair.
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get details about an SSH keypair.
