meta {
  name: Create a new affinity group in a project.
  type: http
  seq: 26
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/affinity-groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "MyAffinityGroup",
    "policy": "anti-affinity"
  }
}
