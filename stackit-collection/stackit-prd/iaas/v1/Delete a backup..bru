meta {
  name: Delete a backup.
  type: http
  seq: 31
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/:projectId/backups/:backupId
  body: none
  auth: inherit
}

params:query {
  force: false
}
