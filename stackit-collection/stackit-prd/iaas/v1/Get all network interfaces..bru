meta {
  name: Get all network interfaces.
  type: http
  seq: 88
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/servers/{{serverId}}/nics
  body: none
  auth: inherit
}
