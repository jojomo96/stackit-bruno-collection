meta {
  name: Remove an image share consumer.
  type: http
  seq: 46
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/{{projectId}}/images/{{imageId}}/share/{{consumerProjectId}}
  body: none
  auth: inherit
}
