meta {
  name: Update information of the snapshot.
  type: http
  seq: 113
}

auth {
  mode: inherit
}

patch {
  url: {{baseUrl}}/v1/projects/:projectId/snapshots/:snapshotId
  body: json
  auth: inherit
}

body:json {
  {
    "labels": {
      "foo": "foobar"
    }
  }
}
