meta {
  name: Set image share.
  type: http
  seq: 45
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v1/projects/{{projectId}}/images/{{imageId}}/share
  body: json
  auth: inherit
}

body:json {
  {
    "parentOrganization": true
  }
}
