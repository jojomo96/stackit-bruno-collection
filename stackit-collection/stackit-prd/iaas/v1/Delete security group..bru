meta {
  name: Delete security group.
  type: http
  seq: 71
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/projects/{{projectId}}/security-groups/{{securityGroupId}}
  body: none
  auth: inherit
}
