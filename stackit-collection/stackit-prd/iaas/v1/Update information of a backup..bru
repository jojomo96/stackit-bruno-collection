meta {
  name: Update information of a backup.
  type: http
  seq: 33
}

auth {
  mode: inherit
}

patch {
  url: {{baseUrl}}/v1/projects/{{projectId}}/backups/{{backupId}}
  body: json
  auth: inherit
}

body:json {
  {
    "labels": {
      "foo": "foobar"
    }
  }
}
