meta {
  name: List all network interfaces inside a network.
  type: http
  seq: 55
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/networks/:networkId/nics
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
