info:
  name: Resize a server.
  type: http
  seq: 95

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/servers/:serverId/resize"
  params:
    - name: projectId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Project.
    - name: serverId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Server.
  body:
    type: json
    data: |-
      {
        "machineType": "t1.1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Resize the server to the given machine type.
