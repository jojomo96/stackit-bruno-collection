meta {
  name: List all network areas in an organization.
  type: http
  seq: 8
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/organizations/:organizationId/network-areas
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
