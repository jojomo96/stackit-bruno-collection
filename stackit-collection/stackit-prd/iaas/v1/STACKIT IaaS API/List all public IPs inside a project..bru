meta {
  name: List all public IPs inside a project.
  type: http
  seq: 62
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/public-ips
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
