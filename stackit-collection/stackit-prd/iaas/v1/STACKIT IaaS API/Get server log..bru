meta {
  name: Get server log.
  type: http
  seq: 85
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/servers/:serverId/log
  body: none
  auth: inherit
}

params:query {
  length: 2000
}
