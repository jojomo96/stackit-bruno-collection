meta {
  name: Associate a public IP to the server.
  type: http
  seq: 92
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v1/projects/:projectId/servers/:serverId/public-ips/:publicIpId
  body: none
  auth: inherit
}
