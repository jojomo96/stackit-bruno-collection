meta {
  name: List all network interfaces inside a project.
  type: http
  seq: 60
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/nics
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
