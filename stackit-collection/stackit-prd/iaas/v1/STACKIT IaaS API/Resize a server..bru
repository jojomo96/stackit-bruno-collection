meta {
  name: Resize a server.
  type: http
  seq: 95
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/:projectId/servers/:serverId/resize
  body: json
  auth: inherit
}

body:json {
  {
    "machineType": "t1.1"
  }
}
