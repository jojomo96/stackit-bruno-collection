meta {
  name: List all network ranges in a network area.
  type: http
  seq: 13
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/organizations/:organizationId/network-areas/:areaId/network-ranges
  body: none
  auth: inherit
}
