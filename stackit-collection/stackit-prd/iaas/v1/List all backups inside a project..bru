meta {
  name: List all backups inside a project.
  type: http
  seq: 29
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/{{projectId}}/backups
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
