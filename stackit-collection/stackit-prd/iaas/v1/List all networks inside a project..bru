meta {
  name: List all networks inside a project.
  type: http
  seq: 50
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/networks
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
