meta {
  name: Delete a network area.
  type: http
  seq: 10
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/organizations/{{organizationId}}/network-areas/{{areaId}}
  body: none
  auth: inherit
}
