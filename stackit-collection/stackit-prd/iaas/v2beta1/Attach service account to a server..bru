meta {
  name: Attach service account to a server.
  type: http
  seq: 113
}

put {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/servers/:serverId/service-accounts/:serviceAccountMail
  body: none
  auth: inherit
}
