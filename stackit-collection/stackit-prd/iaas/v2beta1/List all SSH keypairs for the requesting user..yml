info:
  name: List all SSH keypairs for the requesting user.
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v2beta1/keypairs"
  params:
    - name: label_selector
      value: ""
      type: query
      description: Filter resources by labels.
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a list of all SSH keypairs assigned to the requesting user.
