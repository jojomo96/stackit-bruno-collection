meta {
  name: Delete an Image.
  type: http
  seq: 51
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/images/{{imageId}}
  body: none
  auth: inherit
}
