meta {
  name: Update a network route.
  type: http
  seq: 26
}

patch {
  url: {{baseUrl}}/v2beta1/organizations/:organizationId/network-areas/:areaId/regions/:region/routes/:routeId
  body: json
  auth: inherit
}

body:json {
  {
    "labels": {
      "key": "value"
    }
  }
}
