meta {
  name: Get the affinity group.
  type: http
  seq: 42
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/affinity-groups/{{affinityGroupId}}
  body: none
  auth: inherit
}
