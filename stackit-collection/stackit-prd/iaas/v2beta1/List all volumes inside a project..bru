meta {
  name: List all volumes inside a project.
  type: http
  seq: 129
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/volumes
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
