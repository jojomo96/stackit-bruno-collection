meta {
  name: List all servers inside a project.
  type: http
  seq: 91
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/servers
  body: none
  auth: inherit
}

params:query {
  details: false
  ~label_selector: 
}
