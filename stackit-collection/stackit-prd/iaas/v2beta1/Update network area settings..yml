info:
  name: Update network area settings.
  type: http
  seq: 11

http:
  method: PATCH
  url: "{{baseUrl}}/v2beta1/organizations/:organizationId/network-areas/:areaId"
  params:
    - name: organizationId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Organization.
    - name: areaId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Network Area.
  body:
    type: json
    data: |-
      {
        "labels": {
          "key": "value"
        },
        "name": "My network area"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Update the settings of a network area in an organization.
