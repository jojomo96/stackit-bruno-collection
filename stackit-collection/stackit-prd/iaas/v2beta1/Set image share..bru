meta {
  name: Set image share.
  type: http
  seq: 57
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/images/{{imageId}}/share
  body: json
  auth: inherit
}

body:json {
  {
    "parentOrganization": true
  }
}
