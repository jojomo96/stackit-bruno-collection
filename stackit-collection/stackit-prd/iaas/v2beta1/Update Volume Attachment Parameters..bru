meta {
  name: Update Volume Attachment Parameters.
  type: http
  seq: 120
}

patch {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/servers/:serverId/volume-attachments/:volumeId
  body: json
  auth: inherit
}

body:json {
  {
    "deleteOnTermination": true
  }
}
