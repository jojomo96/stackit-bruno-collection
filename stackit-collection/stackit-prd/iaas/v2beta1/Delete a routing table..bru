meta {
  name: Delete a routing table.
  type: http
  seq: 29
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2beta1/organizations/:organizationId/network-areas/:areaId/regions/:region/routing-tables/:routingTableId
  body: none
  auth: inherit
}
