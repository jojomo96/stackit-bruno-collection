info:
  name: Create new network area in an organization.
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/v2beta1/organizations/:organizationId/network-areas"
  params:
    - name: organizationId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Organization.
  body:
    type: json
    data: |-
      {
        "labels": {
          "key": "value"
        },
        "name": "My network area"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create a new network area in an organization.
