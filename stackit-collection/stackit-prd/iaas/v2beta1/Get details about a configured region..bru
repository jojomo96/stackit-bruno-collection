meta {
  name: Get details about a configured region.
  type: http
  seq: 15
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/organizations/{{organizationId}}/network-areas/{{areaId}}/regions/{{region}}
  body: none
  auth: inherit
}
