meta {
  name: Get security group details.
  type: http
  seq: 85
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/security-groups/{{securityGroupId}}
  body: none
  auth: inherit
}
