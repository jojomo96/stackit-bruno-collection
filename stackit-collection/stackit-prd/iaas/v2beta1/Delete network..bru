meta {
  name: Delete network.
  type: http
  seq: 65
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/networks/:networkId
  body: none
  auth: inherit
}
