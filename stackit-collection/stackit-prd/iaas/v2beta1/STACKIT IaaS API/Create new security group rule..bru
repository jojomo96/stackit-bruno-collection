meta {
  name: Create new security group rule.
  type: http
  seq: 88
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/security-groups/:securityGroupId/rules
  body: json
  auth: inherit
}

body:json {
  {}
}
