meta {
  name: Boot up a server.
  type: http
  seq: 114
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/servers/:serverId/start
  body: none
  auth: inherit
}
