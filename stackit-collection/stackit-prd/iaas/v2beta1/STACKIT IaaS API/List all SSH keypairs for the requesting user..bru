meta {
  name: List all SSH keypairs for the requesting user.
  type: http
  seq: 1
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/keypairs
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
