meta {
  name: Remove image share.
  type: http
  seq: 54
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/images/:imageId/share
  body: none
  auth: inherit
}
