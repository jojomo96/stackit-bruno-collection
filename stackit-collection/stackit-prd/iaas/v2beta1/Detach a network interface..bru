meta {
  name: Detach a network interface.
  type: http
  seq: 102
}

delete {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/servers/:serverId/nics/:nicId
  body: none
  auth: inherit
}
