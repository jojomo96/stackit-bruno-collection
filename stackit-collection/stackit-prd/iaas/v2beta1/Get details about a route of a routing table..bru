meta {
  name: Get details about a route of a routing table.
  type: http
  seq: 35
}

get {
  url: {{baseUrl}}/v2beta1/organizations/:organizationId/network-areas/:areaId/regions/:region/routing-tables/:routingTableId/routes/:routeId
  body: none
  auth: inherit
}
