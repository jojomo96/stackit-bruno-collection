meta {
  name: Reboot the server.
  type: http
  seq: 106
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/servers/{{serverId}}/reboot
  body: none
  auth: inherit
}

params:query {
  action: soft
}
