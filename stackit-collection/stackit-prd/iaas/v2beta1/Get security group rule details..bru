meta {
  name: Get security group rule details.
  type: http
  seq: 90
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/security-groups/{{securityGroupId}}/rules/{{securityGroupRuleId}}
  body: none
  auth: inherit
}
