meta {
  name: Create and attach a network interface from the specified network.
  type: http
  seq: 100
}

post {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/servers/:serverId/networks/:networkId
  body: none
  auth: inherit
}
