meta {
  name: Get details about a volume.
  type: http
  seq: 132
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/volumes/{{volumeId}}
  body: none
  auth: inherit
}
