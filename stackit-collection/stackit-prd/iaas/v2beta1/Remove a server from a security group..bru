meta {
  name: Remove a server from a security group.
  type: http
  seq: 109
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2beta1/projects/{{projectId}}/regions/{{region}}/servers/{{serverId}}/security-groups/{{securityGroupId}}
  body: none
  auth: inherit
}
