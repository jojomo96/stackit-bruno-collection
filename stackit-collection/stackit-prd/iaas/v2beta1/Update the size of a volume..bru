meta {
  name: Update the size of a volume.
  type: http
  seq: 134
}

post {
  url: {{baseUrl}}/v2beta1/projects/:projectId/regions/:region/volumes/:volumeId/resize
  body: json
  auth: inherit
}

body:json {
  {
    "size": 1337
  }
}
