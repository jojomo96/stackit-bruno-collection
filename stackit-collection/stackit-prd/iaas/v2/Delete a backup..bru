meta {
  name: Delete a backup.
  type: http
  seq: 45
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/backups/{{backupId}}
  body: none
  auth: inherit
}

params:query {
  force: false
}
