meta {
  name: Dissociate a public IP from a server.
  type: http
  seq: 104
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/servers/{{serverId}}/public-ips/{{publicIpId}}
  body: none
  auth: inherit
}
