meta {
  name: List all network ranges in a network area.
  type: http
  seq: 18
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/organizations/:organizationId/network-areas/:areaId/regions/:region/network-ranges
  body: none
  auth: inherit
}
