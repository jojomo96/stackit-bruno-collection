meta {
  name: List all routes in a routing table.
  type: http
  seq: 32
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/organizations/{{organizationId}}/network-areas/{{areaId}}/regions/{{region}}/routing-tables/{{routingTableId}}/routes
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
