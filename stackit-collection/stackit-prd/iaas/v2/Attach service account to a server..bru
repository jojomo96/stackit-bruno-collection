meta {
  name: Attach service account to a server.
  type: http
  seq: 113
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/servers/{{serverId}}/service-accounts/{{serviceAccountMail}}
  body: none
  auth: inherit
}
