meta {
  name: List all security groups inside a project.
  type: http
  seq: 82
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/security-groups
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
