meta {
  name: Reboot the server.
  type: http
  seq: 106
}

post {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/reboot
  body: none
  auth: inherit
}

params:query {
  action: soft
}
