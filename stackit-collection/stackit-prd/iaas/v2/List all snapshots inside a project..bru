meta {
  name: List all snapshots inside a project.
  type: http
  seq: 122
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/snapshots
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
