meta {
  name: Create and attach a network interface from the specified network.
  type: http
  seq: 100
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/networks/:networkId
  body: none
  auth: inherit
}
