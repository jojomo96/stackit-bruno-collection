meta {
  name: Stop an existing server.
  type: http
  seq: 115
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/stop
  body: none
  auth: inherit
}
