meta {
  name: List all backups inside a project.
  type: http
  seq: 43
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/backups
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
