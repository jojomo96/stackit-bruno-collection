meta {
  name: Add a server to a security group.
  type: http
  seq: 110
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/servers/{{serverId}}/security-groups/{{securityGroupId}}
  body: none
  auth: inherit
}
