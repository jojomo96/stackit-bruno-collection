meta {
  name: List all networks inside a project.
  type: http
  seq: 63
}

get {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/networks
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
