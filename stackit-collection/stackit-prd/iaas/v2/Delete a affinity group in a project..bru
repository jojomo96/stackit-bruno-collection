meta {
  name: Delete a affinity group in a project.
  type: http
  seq: 41
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/affinity-groups/{{affinityGroupId}}
  body: none
  auth: inherit
}
