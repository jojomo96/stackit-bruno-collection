meta {
  name: List all network routes in a network area.
  type: http
  seq: 22
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/organizations/:organizationId/network-areas/:areaId/regions/:region/routes
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
