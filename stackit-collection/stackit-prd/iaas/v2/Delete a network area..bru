meta {
  name: Delete a network area.
  type: http
  seq: 9
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/organizations/:organizationId/network-areas/:areaId
  body: none
  auth: inherit
}
