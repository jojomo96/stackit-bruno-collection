meta {
  name: List all configured regions in a network area.
  type: http
  seq: 13
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/organizations/{{organizationId}}/network-areas/{{areaId}}/regions
  body: none
  auth: inherit
}
