info:
  name: Create new routing table in a network area.
  type: http
  seq: 28

http:
  method: POST
  url: "{{baseUrl}}/v2/organizations/:organizationId/network-areas/:areaId/regions/:region/routing-tables"
  params:
    - name: organizationId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Organization.
    - name: areaId
      value: d61a8564-c8dd-4ffb-bc15-143e7d0c85ed
      type: path
      description: The identifier (ID) of a STACKIT Network Area.
    - name: region
      value: eu01
      type: path
      description: The STACKIT Region of the resources.
  body:
    type: json
    data: |-
      {
        "createdAt": "",
        "default": false,
        "description": "cool description dude. Wanna play some Snakeybus?",
        "dynamicRoutes": false,
        "id": "",
        "labels": {
          "key": "value"
        },
        "name": "objectName26",
        "systemRoutes": false,
        "updatedAt": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create a new routing table in an existing network area.
