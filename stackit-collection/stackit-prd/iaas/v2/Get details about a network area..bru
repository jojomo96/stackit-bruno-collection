meta {
  name: Get details about a network area.
  type: http
  seq: 10
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/organizations/{{organizationId}}/network-areas/{{areaId}}
  body: none
  auth: inherit
}
