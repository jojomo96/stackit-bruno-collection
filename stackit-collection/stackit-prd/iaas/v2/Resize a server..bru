meta {
  name: Resize a server.
  type: http
  seq: 108
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/resize
  body: json
  auth: inherit
}

body:json {
  {
    "machineType": "t1.1"
  }
}
