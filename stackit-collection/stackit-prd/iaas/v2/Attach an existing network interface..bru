meta {
  name: Attach an existing network interface.
  type: http
  seq: 103
}

put {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/nics/:nicId
  body: none
  auth: inherit
}
