meta {
  name: Delete a volume.
  type: http
  seq: 131
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/projects/{{projectId}}/regions/{{region}}/volumes/{{volumeId}}
  body: none
  auth: inherit
}
