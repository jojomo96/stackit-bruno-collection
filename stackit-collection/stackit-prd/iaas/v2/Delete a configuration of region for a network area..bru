meta {
  name: Delete a configuration of region for a network area.
  type: http
  seq: 14
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/organizations/{{organizationId}}/network-areas/{{areaId}}/regions/{{region}}
  body: none
  auth: inherit
}
