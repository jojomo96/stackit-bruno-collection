meta {
  name: Delete network.
  type: http
  seq: 14
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2alpha1/projects/:projectId/regions/:region/networks/:networkId
  body: none
  auth: inherit
}
