meta {
  name: List all routing tables in a network area.
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v2alpha1/organizations/:organizationId/network-areas/:areaId/regions/:region/routing-tables
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}
