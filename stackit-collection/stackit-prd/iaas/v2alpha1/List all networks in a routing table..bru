meta {
  name: List all networks in a routing table.
  type: http
  seq: 6
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2alpha1/organizations/{{organizationId}}/network-areas/{{areaId}}/regions/{{region}}/routing-tables/{{routingTableId}}/networks
  body: none
  auth: inherit
}
