info:
  name: GetServiceStatus
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/services/:serviceId"
  params:
    - name: projectId
      value: ""
      type: path
    - name: serviceId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: returns the current status of a service in a project.
