meta {
  name: EnableServiceRegional
  type: http
  seq: 4
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/services/:serviceId
  body: none
  auth: inherit
}
