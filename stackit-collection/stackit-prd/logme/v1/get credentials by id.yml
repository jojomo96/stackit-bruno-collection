info:
  name: get credentials by id
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/credentials/:credentialsId"
  params:
    - name: projectId
      value: ""
      type: path
    - name: instanceId
      value: ""
      type: path
      description: Instance id
    - name: credentialsId
      value: ""
      type: path
      description: Credentials id of credentials to fetch
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: get a service credentials by credentials id
