meta {
  name: UpdateACL
  type: http
  seq: 11
}

auth {
  mode: inherit
}

put {
  url: {{baseUrl}}/v1/projects/{{projectId}}/instances/{{instanceId}}/acls/{{aclId}}
  body: json
  auth: inherit
}

body:json {
  {
    "cidr": ""
  }
}
