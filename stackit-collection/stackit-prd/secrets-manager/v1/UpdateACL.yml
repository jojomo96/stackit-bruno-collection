info:
  name: UpdateACL
  type: http
  seq: 11

http:
  method: PUT
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/acls/:aclId"
  params:
    - name: projectId
      value: ""
      type: path
      description: The STACKIT portal project UUID the Secrets Manager instance is part of.
    - name: instanceId
      value: ""
      type: path
      description: The Secrets Manager instance UUID.
    - name: aclId
      value: ""
      type: path
      description: The acl UUID of the Secrets Manager instance.
  body:
    type: json
    data: |-
      {
        "cidr": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Updates the ip ranges for the acl.
