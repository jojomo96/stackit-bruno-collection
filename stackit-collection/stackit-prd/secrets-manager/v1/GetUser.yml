info:
  name: GetUser
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/users/:userId"
  params:
    - name: projectId
      value: ""
      type: path
      description: The STACKIT portal project UUID the Secrets Manager instance is part of.
    - name: instanceId
      value: ""
      type: path
      description: The Secrets Manager instance UUID.
    - name: userId
      value: ""
      type: path
      description: The user UUID with permissions on the Secrets Manager instance.
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Returns the details for the given user.
