info:
  name: Delete an existing role binding (POST)
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/v1alpha/region/:region/secret-group/:resourceId:deleteRoleBinding"
  params:
    - name: region
      value: ""
      type: path
    - name: resourceId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "role": "owner",
        "subject": "user@mail.schwarz"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete a role binding.
