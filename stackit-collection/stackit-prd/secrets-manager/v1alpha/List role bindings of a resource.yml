info:
  name: List role bindings of a resource
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/v1alpha/region/:region/instance/:resourceId:listRoleBindings"
  params:
    - name: region
      value: ""
      type: path
    - name: resourceId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List role bindings of the given resource.
