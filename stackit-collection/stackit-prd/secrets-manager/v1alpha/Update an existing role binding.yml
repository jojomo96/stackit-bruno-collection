info:
  name: Update an existing role binding
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/v1alpha/region/:region/instance/:resourceId:updateRoleBinding"
  params:
    - name: region
      value: ""
      type: path
    - name: resourceId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "role": "owner",
        "subject": "user@mail.schwarz"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Update a role binding.
