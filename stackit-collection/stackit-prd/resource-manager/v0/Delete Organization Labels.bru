meta {
  name: Delete Organization Labels
  type: http
  seq: 8
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v2/organizations/:containerId/labels
  body: none
  auth: inherit
}

params:query {
  ~keys: []
}
