info:
  name: Reset User
  type: http
  seq: 23

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/users/:userId/reset"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
    - name: instanceId
      value: ""
      type: path
      description: instance id
    - name: userId
      value: ""
      type: path
      description: user id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: resets mongodb user's password
