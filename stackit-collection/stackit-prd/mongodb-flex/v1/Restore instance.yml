info:
  name: Restore instance
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/restores"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
    - name: instanceId
      value: ""
      type: path
      description: instance id
  body:
    type: json
    data: |-
      {
        "backupId": "",
        "instanceId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Restore an instance based on snapshot
