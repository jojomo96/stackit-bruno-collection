meta {
  name: Get backup
  type: http
  seq: 12
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/instances/:instanceId/backups/:backupId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  instanceId: {{instanceId}}
  backupId: {{backupId}}
}
