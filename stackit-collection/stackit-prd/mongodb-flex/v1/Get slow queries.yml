info:
  name: Get slow queries
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/advisor/slow-queries"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
    - name: instanceId
      value: ""
      type: path
      description: instance id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: gets slow queries from the Opsmanager performance advisor
