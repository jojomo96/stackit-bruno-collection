info:
  name: List backup
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/instances/:instanceId/backups"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
    - name: instanceId
      value: ""
      type: path
      description: instance id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List backups of an instance
