info:
  name: Get versions
  type: http
  seq: 25

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/versions"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/v1/projects/:projectId/versions"
      method: GET
      params:
        - name: projectId
          value: ""
          type: path
          description: project id
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "versions": [
              "1.0"
            ]
          }
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/v1/projects/:projectId/versions"
      method: GET
      params:
        - name: projectId
          value: ""
          type: path
          description: project id
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "code": 404,
            "fields": {},
            "message": "database not found",
            "type": ""
          }

docs: returns all available versions for creating endpoint
