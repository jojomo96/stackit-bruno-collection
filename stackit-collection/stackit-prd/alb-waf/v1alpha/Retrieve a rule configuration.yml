info:
  name: Retrieve a rule configuration
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/v1alpha/projects/:projectId/regions/:region/rules/:name"
  params:
    - name: projectId
      value: ""
      type: path
    - name: region
      value: ""
      type: path
    - name: name
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: To retrieve an existing rule configuration this endpoint can be used.
