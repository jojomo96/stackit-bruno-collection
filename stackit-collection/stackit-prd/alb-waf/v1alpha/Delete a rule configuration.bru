meta {
  name: Delete a rule configuration
  type: http
  seq: 8
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1alpha/projects/{{projectId}}/regions/{{region}}/rules/{{name}}
  body: none
  auth: inherit
}

params:path {
  projectId: 
  region: 
  name: 
}
