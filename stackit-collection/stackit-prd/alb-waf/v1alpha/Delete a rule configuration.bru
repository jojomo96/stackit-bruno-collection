meta {
  name: Delete a rule configuration
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/v1alpha/projects/:projectId/regions/:region/rules/:name
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  name: {{name}}
}
