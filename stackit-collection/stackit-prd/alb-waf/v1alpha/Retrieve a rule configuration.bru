meta {
  name: Retrieve a rule configuration
  type: http
  seq: 9
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v1alpha/projects/:projectId/regions/:region/rules/:name
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  name: {{name}}
}
