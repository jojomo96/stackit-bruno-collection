meta {
  name: Retrieve a WAF configuration
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/v1alpha/projects/:projectId/regions/:region/wafs/:name
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  name: {{name}}
}
