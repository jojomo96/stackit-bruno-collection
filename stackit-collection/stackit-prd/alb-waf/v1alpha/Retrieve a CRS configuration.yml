info:
  name: Retrieve a CRS configuration
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/v1alpha/projects/:projectId/regions/:region/core-rule-sets/:name"
  params:
    - name: projectId
      value: ""
      type: path
    - name: region
      value: ""
      type: path
    - name: name
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: To retrieve an existing core rule set (CRS) configuration this endpoint can be used.
