meta {
  name: List connections for an existing VPN gateway.
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/v1alpha1/projects/:projectId/regions/:region/gateways/:gatewayId/connections
  body: none
  auth: inherit
}

params:query {
  ~label_selector: 
}

params:path {
  projectId: {{projectId}}
  region: eu01
  region: eu02
  gatewayId: {{gatewayId}}
}
