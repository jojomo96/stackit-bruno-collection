meta {
  name: Create space
  type: http
  seq: 14
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/projects/{{projectId}}/regions/{{region}}/organizations/{{organizationId}}/spaces
  body: json
  auth: inherit
}

params:path {
  projectId: 
  region: 
  organizationId: 
}

body:json {
  {
    "name": "space0"
  }
}
