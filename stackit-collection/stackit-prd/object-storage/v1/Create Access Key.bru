meta {
  name: Create Access Key
  type: http
  seq: 4
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v1/project/:projectId/access-key
  body: json
  auth: inherit
}

params:query {
  ~credentials-group: 
}

params:path {
  projectId: {{projectId}}
}

body:json {
  {
    "expires": "2020-09-04T00:00:00.000Z"
  }
}
