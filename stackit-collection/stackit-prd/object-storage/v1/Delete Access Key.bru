meta {
  name: Delete Access Key
  type: http
  seq: 5
}

auth {
  mode: inherit
}

delete {
  url: {{baseUrl}}/v1/project/:projectId/access-key/:keyId
  body: none
  auth: inherit
}

params:query {
  ~credentials-group: 
}

params:path {
  projectId: {{projectId}}
  keyId: {{keyId}}
}
