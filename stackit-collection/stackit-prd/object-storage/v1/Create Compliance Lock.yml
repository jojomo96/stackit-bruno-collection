info:
  name: Create Compliance Lock
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseUrl}}/v1/project/:projectId/compliance-lock"
  params:
    - name: projectId
      value: cd5e788d-5b7b-4ab9-a20d-e790205df10b
      type: path
      description: STACKIT project ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Enable compliance lock for a project.
