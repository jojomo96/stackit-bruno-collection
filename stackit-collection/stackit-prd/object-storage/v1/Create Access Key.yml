info:
  name: Create Access Key
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/v1/project/:projectId/access-key"
  params:
    - name: projectId
      value: cd5e788d-5b7b-4ab9-a20d-e790205df10b
      type: path
      description: STACKIT project ID
    - name: credentials-group
      value: ""
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "expires": "2020-09-04T00:00:00.000Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Create an access key for the given project.
