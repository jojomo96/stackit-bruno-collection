meta {
  name: Delete Credentials Group
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/v1/project/:projectId/credentials-group/:groupId
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  groupId: {{groupId}}
}
