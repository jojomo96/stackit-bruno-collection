info:
  name: Create Project
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/v1/project/:projectId"
  params:
    - name: projectId
      value: cd5e788d-5b7b-4ab9-a20d-e790205df10b
      type: path
      description: STACKIT project ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Create a project. Creation will also be successful if the project
  already exists, but will not create a duplicate
