meta {
  name: Create Bucket
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/v2/project/:projectId/regions/:region/bucket/:bucketName
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  bucketName: {{bucketName}}
}
