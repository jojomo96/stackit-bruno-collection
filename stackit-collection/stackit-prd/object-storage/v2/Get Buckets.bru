meta {
  name: Get Buckets
  type: http
  seq: 10
}

auth {
  mode: inherit
}

get {
  url: {{baseUrl}}/v2/project/:projectId/regions/:region/buckets
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
}
