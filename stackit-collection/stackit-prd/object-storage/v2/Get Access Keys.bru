meta {
  name: Get Access Keys
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/v2/project/:projectId/regions/:region/access-keys
  body: none
  auth: inherit
}

params:query {
  ~credentials-group: 
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
}
