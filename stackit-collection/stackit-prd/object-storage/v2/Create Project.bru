meta {
  name: Create Project
  type: http
  seq: 3
}

auth {
  mode: inherit
}

post {
  url: {{baseUrl}}/v2/project/:projectId/regions/:region
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
}
