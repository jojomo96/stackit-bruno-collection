meta {
  name: Create Project
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v2/project/:projectId/regions/:region
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
}
