meta {
  name: Delete Bucket
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/v2/project/:projectId/regions/:region/bucket/:bucketName
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  region: {{region}}
  bucketName: {{bucketName}}
}
