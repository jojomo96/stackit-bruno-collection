info:
  name: get list of updates
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/servers/:serverId/updates"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
    - name: serverId
      value: ""
      type: path
      description: server id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
