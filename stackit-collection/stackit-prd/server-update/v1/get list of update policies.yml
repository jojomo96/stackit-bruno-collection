info:
  name: get list of update policies
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/v1/projects/:projectId/update-policies"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
