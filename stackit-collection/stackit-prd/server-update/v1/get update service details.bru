meta {
  name: get update service details
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/projects/:projectId/servers/:serverId/service
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  serverId: {{serverId}}
}
