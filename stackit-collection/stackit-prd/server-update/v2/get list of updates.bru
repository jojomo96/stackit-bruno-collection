meta {
  name: get list of updates
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/updates
  body: none
  auth: inherit
}

params:path {
  projectId: {{projectId}}
  serverId: {{serverId}}
  region: {{region}}
}
