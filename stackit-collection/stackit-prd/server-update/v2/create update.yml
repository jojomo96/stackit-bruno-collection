info:
  name: create update
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/v2/projects/:projectId/regions/:region/servers/:serverId/updates"
  params:
    - name: projectId
      value: ""
      type: path
      description: project id
    - name: serverId
      value: ""
      type: path
      description: server id
    - name: region
      value: eu01
      type: path
      description: region
  body:
    type: json
    data: |-
      {
        "backupBeforeUpdate": false,
        "maintenanceWindow": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
